{
  "name": "ohentai-downloader",
  "version": "1.0.7",
  "description": "Tampermonkey userscript for downloading videos from ohentai.org",
  "scripts": {
    "build": "tsx build/build.ts",
    "dev": "tsx build/build.ts --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0"
  }
}
